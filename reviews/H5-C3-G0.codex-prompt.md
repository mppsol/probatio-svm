# Codex review payload — H5 · C3 G0 pre-registration

**Not yet run.** Prepared by CC per the founder ruling of 2026-08-22. Invoke exactly as below,
read-only, **one round**. **The founder decides whether it runs.**

```sh
/Applications/ChatGPT.app/Contents/Resources/codex exec \
  -C /Users/hiroyusai/src/probatio-svm -s read-only \
  "$(cat reviews/H5-C3-G0.codex-prompt.md)" < /dev/null
```

Cross-review is clean: **CC wrote every line under review.** Codex wrote `crates/h4-sentinel`, whose
frozen evidence is cited as input, and Codex's own prior review is what kept C3 alive — it should say
where it is now reviewing a consequence of its own earlier finding.

---

You are the INDEPENDENT REVIEWER of the **H5 · C3 G0 pre-registration** in
/Users/hiroyusai/src/probatio-svm (branch `h5/agent-release-tests-g0`). Read-only: write nothing, run
no harness or simulation, no network, do not touch `/Users/hiroyusai/src/solvo`.

Read as binding: **`docs/H5-C3-G0.md`** (the object of this review), `docs/H5-GATE.md`,
`docs/H5-CANDIDATES.md` (§5 is C3's origin; §8 governs), `reviews/H5-G0-candidates.md` (your prior
review — the `DISSENT` that kept C3 alive), `STATUS.md`, `docs/decisions/H4-sentinel-kill.md`,
`evidence/h4-sentinel.json`, `fixtures/h4/`.

## Context

Four hypotheses are dead in this repo, and H5 has already lost three candidates (§8.1, C1, C2) —
**none of which may be revived, and none of which this review may resurrect.** C3 survived your prior
review, and the founder has now **fixed the baseline before measurement**:

- `Pol` = a static policy (destination allowlist + per-transaction cap);
- `Sim` = a **non-persistent** single `simulateTransaction`;
- **neither may retain, apply or chain post-state between transactions**;
- an executor that carries cloned state across transactions **is not a baseline — it is H5 itself**.

The founder required seven freeze items and added a hard condition: **if three *meaningful* capital
transactions cannot be constructed non-arbitrarily — no filler, no no-op, no decorative transaction —
C3 dies at the design gate and is never measured.**

Nothing has been implemented or measured. **Your job is to decide whether this pre-registration is
tight enough to produce an honest verdict, and whether C3 should be measured at all.**

## Answer each, with an explicit verdict

1. **Freeze item 1 — are there really three *meaningful* transactions?** CC counts **capital actions
   only** (`W1`, `W2`, `W3`, each a 100,000,000-atom withdrawal) and **explicitly excludes klend's
   refresh preambles from the count**, bundled or not. Is that honest, or is the **two-tranche plan
   itself padding** invented to reach three? The natural C3 is one planned action plus one duplicate
   (= 2). Say plainly whether chunking a 200,000,000 withdrawal into two tranches is ordinary agent
   behaviour or 件数合わせ. **If it is padding, C3 must die here.**
2. **Freeze item 2 — is the fault injection legitimate and correctly located?** It sets only the
   *agent-facing* result of `W2` to `Timeout` while the VM commits `W2` normally. Does anything in
   §3 let the injection touch chain state, or leak into `W1`/`W3`? Is "the chain is not lied to; the
   agent is" actually true as specified?
3. **Freeze item 3 — is the duplicate-signature guard sufficient?** `W3` uses a new blockhash and the
   harness asserts `sig(W3) ≠ sig(W2)`. Is there any path where the episode accidentally tests the
   runtime's dedup instead of the agent, or where LiteSVM's behaviour differs from a validator's in a
   way that changes the result?
4. **Freeze item 4 — are the agent rules complete and deterministic?** Could the defective rule loop
   forever, or terminate differently than stated? Is the "correct rule" a fair comparator, or is it
   strawmanned so the defect looks worse than it is?
5. **Freeze item 5 — is the predicate right?** `P1 ∧ P2 ∧ P3`, cumulative, state-bytes-only. Does
   `P3` (every individual transaction is innocent) do the work CC claims — proving no
   per-transaction check could have flagged it? Can the predicate be satisfied by something that is
   **not** the failure C3 claims?
6. **Freeze item 6 — are the baselines fairly specified, and is the 150,000,000 cap fair?** It sits
   above one tranche and below two. Is that a cap a real operator would set, or is it reverse-engineered
   to miss? **Would any reasonable static policy catch a duplicate ordinary transaction?** And under
   the §0 ruling, is there any way `Sim` sees `P1`/`P2`? If either baseline can catch it, `KILL-1`
   fires now.
7. **Freeze item 7 — is anything left unpinned** that a later run could choose after seeing a result?
   Check the binary selection (`klend_new` only), the hashes, the two mutations, the slots, and the
   entrypoint's exit-code contract.
8. **The `KILL-2` risk.** CC predicts the likeliest death is `W3` being refused or clamped. From
   H4's frozen evidence (case A moved exactly 100,000,000; case E's maximum was 486,657,686), is
   `3 × 100,000,000 = 300,000,000` genuinely inside the ceiling **after two prior withdrawals have
   already changed the position**? CC treats H4's single-call ceiling as headroom for a *sequence* —
   is that sound, or is it the same unproven path-independence assumption you flagged as P1 last
   round?
9. **Does C3 still clear condition 2 under the founder's §0 ruling**, and is the ruling faithfully
   applied in §7, or has CC quietly given `Sim` less than the ruling allows (it may fully inspect
   returned post-state; it merely may not chain)?
10. **The one change** that would most reduce the chance of a dishonest `GO`. And separately: **should
    C3 be measured at all**, or is there a defect that makes the measurement predictably uninformative?

## Output

Print (do not write files) a Markdown review to be saved verbatim as `reviews/H5-C3-G0.md`:

- **Verdict line**: `APPROVE — measure it` / `CHANGES` (list what must be frozen or fixed first) /
  `KILL AT DESIGN GATE` (name which condition, and why no fix is available).
- P0 / P1 / P2 findings with file:line. A **P0** is anything that could produce a `GO` C3 has not
  earned, anything that means C3 is already dead, or padding in the three-transaction count.
- A direct answer to each of the ten questions.
- What you could not verify read-only.

Do not help C3 survive, and do not kill it to be safe. Three candidates have died here; a fourth
false start and a wrongly-killed live candidate are both expensive, and only one of them is visible
later.
